import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sumitprasadsaha.tuitionmanager',
  appName: 'Tuition Manager',
  webDir: 'dist'
};

export default config;
